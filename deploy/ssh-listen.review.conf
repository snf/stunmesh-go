# Administrator-reviewed NAS-only drop-in; NOT installed by any helper.
# Proposed /etc/ssh/sshd_config.d/40-nas-listen.conf.
# ssh.socket was inactive and ssh.service active at the 2026-09-23 check.
# Recheck those facts and effective config before installation.
ListenAddress 127.0.0.1
ListenAddress 192.168.0.10
