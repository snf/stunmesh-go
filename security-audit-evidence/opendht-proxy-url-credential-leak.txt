2026-09-13. Synthetic credentials only; no production proxy or secret was used.

Command (isolated bubblewrap namespace, offline):
  go test -tags security_audit,builtin_opendht -run '^TestAuditOpenDHTProxyURLCredentialsReachFailureError$' -v ./internal/plugin/builtin/opendht
Result: PASS. A local httptest proxy returned HTTP 503. The OpenDHT plugin's
returned error contained the configured URL's synthetic password.
The same targeted test passed under Go's race detector.

Relevant source paths:
  internal/plugin/builtin/opendht/opendht.go:175-189 logs the full endpoint
  and wraps each request error with the full endpoint URL.
  internal/ctrl/establish.go:68-70 logs this error on desktop.
  mobile/controller.go:301-305 forwards this error to EventListener.OnLog.
  stunmesh-android TunnelManager.onLog appends it to the rolling log;
  StatusScreen.buildLogExport writes those log lines without redaction.

Precondition: an OpenDHT proxy URL with userinfo credentials is configured,
and an ordinary request fails. The default public proxy URL has no userinfo.
The exposure is local logs or a user-shared log export; no remote exfiltration
path was demonstrated. A related Android test proves that even the redacted
configuration section of a log export retains URL userinfo.
